# AI 泡沫仪表盘

判断本轮美股 AI 行情处于泡沫哪个阶段的个人研究工具。纯静态网页，无框架无依赖，
双击 `index.html` 即可使用。

## 快速开始

**日常使用：双击 `启动仪表盘.command`** —— 自动检查数据新鲜度（超过 12 小时
就重新抓取，约半分钟），然后打开页面。

手动方式：

```bash
python3 fetch_data.py     # 强制拉取最新数据，生成 data/data.js
open index.html           # 直接用浏览器打开（无需起服务器）
```

> 为什么不让网页自己抓数据：浏览器跨域限制（CORS）不允许本地页面直接读
> FRED 的接口，所以抓取必须由脚本完成，启动器把两步打包成了一步。

## 评估模型（双轴 × 五阶段）

- **泡沫程度（干柴）**：席勒 CAPE、巴菲特指标、市场集中度、资本开支增速/强度、
  发债规模、循环融资、AI 落地率、一级市场狂热度（IPO 热）、监管警告
  —— 决定跌下来有多惨
- **引爆临近度（火星）**：高收益债利差及其 3 个月变化、CCC 级利差、金融条件指数、
  收益率曲线、VIX、200 日线乖离、纽交所净新高（宽度背离）、新股供给抽水、
  FCF 转负厂商数 —— 决定顶点何时出现

其中「新股供给抽水」升到 3 级（融资繁荣而指数滞涨）是本轮周期需要最优先响应的
顶部信号；「净新高」需每周从 Reuters/交易所数据人工更新一次。

每个指标按分段阈值插值为 0-100 分，加权合成两轴得分，再映射到
金德尔伯格－明斯基五阶段（萌芽 → 繁荣 → 亢奋 → 警讯 → 恐慌）。

阈值和权重都在 `index.html` 的 `INDICATORS` 配置里，可自行调整。

## 数据更新

| 类型 | 怎么更新 | 频率建议 |
|------|----------|----------|
| 自动指标（FRED：利差/VIX/金融条件/标普500 等） | `python3 fetch_data.py` | 每周或想看时 |
| 席勒 CAPE | 脚本自动抓 multpl.com，失败则用手填值 | 自动 |
| 手动指标（资本开支、循环融资、集中度等） | 编辑 `data/manual.json` 后重跑脚本 | 每季度财报季后 |
| 关键事件时间线 | `data/manual.json` 的 `events` | 随手记 |

## 踩坑记录

- **FRED 防火墙掐伪装 UA**：`fredgraph.csv` 免 key，但请求头里如果带
  `Mozilla/5.0 ...` 这类伪装浏览器的 User-Agent，会被 WAF 掐掉
  （表现为超时或 HTTP/2 INTERNAL_ERROR），用 curl 默认 UA 反而秒回。
  脚本对 FRED 已不带自定义 UA，另有 2 秒间隔 + 失败退避重试兜底。
- **macOS Python SSL 证书**：系统 Python 缺根证书会报
  `CERTIFICATE_VERIFY_FAILED`，脚本优先走系统 `curl` 绕开（和 call-copilot
  项目踩过的是同一个坑）。

## 重点盯什么（模型之外的人肉信号）

1. 高收益债利差站上 **4%** 且不回落 —— 资金链收紧确认
2. 任何一家超大规模云厂商**下调资本开支指引** —— 2000 年 3 月的剧本
3. AI 相关**发债利率跳升或发行遇冷** —— 融资窗口关闭之日即游戏结束之时

免责声明：个人研究工具，评分模型为主观设定，不构成投资建议。
